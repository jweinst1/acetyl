string(TIMESTAMP ACETYL_BUILD_TIME PARENT_SCOPE)

# This sets the package specific versioning
set(ACETYL_MAJOR_VERSION 0)
set(ACETYL_MINOR_VERSION 1)
set(ACETYL_PATCH_VERSION 0)

# This sets the name used in installation and packaging 
set(ACETYL_NAME acetyl)
set(ACETYL_NAMED_VERSION "${ACETYL_NAME}-${ACETYL_MAJOR_VERSION}.${ACETYL_MINOR_VERSION}.${ACETYL_PATCH_VERSION}")

set(ACETYL_NAMED_VERSION ${ACETYL_NAMED_VERSION} PARENT_SCOPE)
